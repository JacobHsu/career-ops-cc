# Personal data (user fills these)
cv.md
cv.zh.md
data/applications.md
data/pipeline.md
data/scan-history.tsv
data/scan-history.md
reports/*.md
!reports/.gitkeep
interview-prep/goodjob/
output/*
!output/.gitkeep
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/jds/*
!batch/jds/.gitkeep
batch/tracker-additions/**/*.tsv
!batch/tracker-additions/.gitkeep
jds/*
!jds/.gitkeep

# Writing samples (user's personal files — never committed)
writing-samples/*
!writing-samples/README.md

# Personal cover letter examples — never committed
examples/coverletter/

# User config and customization (never auto-updated)
config/profile.yml
portals.yml
portals.parttime.yml
modes/_profile.md
.update-dismissed
.update-lock

# Secrets (never commit — use .env.example as template)
.env

# Generated
.resolved-prompt-*
node_modules/
bun.lock

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard
package-lock.json
# Personal resume
config/resume.pdf
resume.pdf

# Import folder — drop personal files here to be read by AI, nothing will be committed
import/*
!import/.gitkeep
